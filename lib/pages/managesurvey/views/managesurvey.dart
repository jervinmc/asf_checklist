import 'package:flutter/material.dart';

class ManageSurvey extends StatelessWidget {
  const ManageSurvey({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Manage Survey'),),
      body: Container(
      
      ),
    );
  }
}
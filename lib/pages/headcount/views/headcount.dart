import 'package:flutter/material.dart';

class HeadCount extends StatelessWidget {
  const HeadCount({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Swine Head Count'),),
      body: Container(
        child: Column(
          children: [

          ],
        ),
      ),
    );
  }
}